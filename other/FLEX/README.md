# FLEX - SFL

### Server
```commandline
python3 server.py
```
### Client 
```commandline
python3 client.py --layer_id 1 --c 0 --s True
```
* ``layer_id`` : ID of layer, start from 1
* ``c``: ID cluster by device
* ``s``: Select/Reject device

